# Docker Compose file for Vapor
#
# Install Docker on your system to run and test
# your Vapor app in a production-like environment.
#
# Note: This file is intended for testing and does not
# implement best practices for a production deployment.
#
# Learn more: https://docs.docker.com/compose/reference/
#
#   Build images: docker compose build
#      Start app: docker compose up app
# Start database: docker compose up db
# Run migrations: docker compose run migrate
#       Stop all: docker compose down (add -v to wipe db)
#

volumes:
  swift_blogs_data:

x-shared_environment: &shared_environment
  LOG_LEVEL: ${LOG_LEVEL:-debug}
  DATABASE_HOST: db
  DATABASE_NAME: swiftblogs
  DATABASE_USERNAME: swiftblogs
  DATABASE_PASSWORD: swiftblogs
  
services:
  db:
    image: mariadb:10
    volumes:
      - swift_blogs_data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=swiftblogs
      - MYSQL_DATABASE=swiftblogs
      - MYSQL_USER=swiftblogs
      - MYSQL_PASSWORD=swiftblogs
    ports:
      - '3306:3306'
